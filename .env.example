# La SEULE variable obligatoire : le token du bot
# (Developer Portal > Bot > Reset Token)
TOKEN=colle_ton_token_ici

# (Optionnel) Dossier de stockage de la config, pour la persistance sur Railway.
# Crée un Volume Railway monté sur /data puis décommente la ligne ci-dessous :
# DATA_DIR=/data
